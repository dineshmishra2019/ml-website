apiVersion: v1
kind: Service
metadata:
  name: {{ include "ml-playground.fullname" . }}
  labels:
    {{- include "ml-playground.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ml-playground.selectorLabels" . | nindent 4 }